// src/help/topics.ts — ヘルプの本文。
//
// 画面から直接たどれる場所に置く。README を読みに行かせない
// （「Podman 未構築」と出ている人が知りたいのはその場での直し方で、
//  リポジトリの構成説明ではない）。
//
// 本文は Markdown。react-markdown でそのまま描く。
// **手順は実装に合わせて書くこと。** sandbox.py / config.py の挙動と
// ズレると、ヘルプを読んだ人が余計な作業をする。

import { BRAND_DESCRIPTION, BRAND_NAME } from '../brand'

export interface HelpTopic {
  slug: string
  title: string
  en: string
  summary: string
  body: string
}

export const HELP_TOPICS: HelpTopic[] = [
  {
    slug: 'about',
    title: `${BRAND_NAME} とは`,
    en: `About ${BRAND_NAME}`,
    summary: 'このサービスが何をするものか。名前と、内部名がまだ旧名である理由。',
    body: `
## ${BRAND_NAME}（めつけ）

${BRAND_DESCRIPTION}

やることを日本語で書いて渡すと、LLM が自分で検索し、必要ならコードを書いて
実行し、結果をまとめて返します。cron を登録しておけば定期的に走ります。

**データを外に出さない**ことを前提に作っています。ローカルLLM だけで
使う構成なら、外へ出る通信は Web 検索だけです。

## 名前の由来

「目付（めつけ）」— 見張る役、見て確かめる役のこと。
このアプリの中身も同じで、エージェントが出した答えをそのまま信じず、
別のレビュアーが数値や出典を機械的に照合してから返します。

## できること

| | 何をするもの | 向いていること |
| --- | --- | --- |
| 🔧 ツール | 保存した Python を毎回同じように実行する | 集計・変換・定型処理 |
| 🧠 エージェント | LLM がその場で判断して調べ、まとめる | 調査・要約・監視 |

どちらも一覧（タスク）から実行でき、cron で定期実行を登録できます。
コードの実行は**すべて** Podman のコンテナ内で行われます。

## 内部の名前がまだ \`agent_studio\` なのはなぜか

リポジトリ名は \`Metsuke\` に変わりましたが、保存先のファイル名
（\`agent_studio.db\`）、環境変数（\`AGENT_STUDIO_DB\`）、サンドボックスの
イメージ名（\`agent-studio-sandbox:latest\`）は**旧名のまま**にしてあります。

これらを変えると、すでに使っている環境で保存済みの設定・タスク・実行履歴が
見つからなくなり、移行作業が必要になるためです。表示名とリポジトリ名だけを
${BRAND_NAME} に変えて、データの互換性は保っています。
`.trim(),
  },
  {
    slug: 'podman',
    title: 'サンドボックスの構築',
    en: 'Sandbox / Podman',
    summary: 'ツールのコード実行に使うコンテナ。「Podman 未構築」と出たときはここ。',
    body: `
## これは何か

ツールとエージェントが生成した Python コードは、**すべて Podman のコンテナ内で実行**されます。
ホスト上では実行しません。生成コードが何をするか事前に分からないためです。

そのコンテナ用のイメージが未作成、または古いときに「Podman 未構築」と表示されます。

## サイドバーの表示の意味

| 表示 | 状態 | 対応 |
| --- | --- | --- |
| Podman OK | イメージが最新（\`ready\`） | 何もしなくてよい |
| Podman 未構築 | 未作成（\`missing\`）／古い（\`stale\`）／podman が無い | 下記のとおり |

## 1. Podman を入れる

\`\`\`bash
# Ubuntu / Debian
sudo apt install podman

# macOS
brew install podman
podman machine init && podman machine start
\`\`\`

入ったか確認します。

\`\`\`bash
podman --version
\`\`\`

## 2. イメージを作る

**手動でビルドする必要は基本ありません。**
ライブラリ画面で「プレビュー実行」を1回押すと、そのときに自動でビルドされます
（数分かかります。**このときだけネットワークが必要**です）。

先に済ませておきたい場合は、リポジトリのルートで次を実行します。

\`\`\`bash
podman build -t agent-studio-sandbox:latest .
\`\`\`

## 「古い（stale）」と出る場合

\`requirements-tools.txt\` を変更すると、イメージが自動で作り直されます。
これも次回のプレビュー実行時に自動で行われるので、手動作業は要りません。

## なぜ素の python イメージではないのか

\`python:3.12-slim\` には標準ライブラリしか入っていません。
サンドボックスは通信を遮断するので**コンテナ内で \`pip install\` ができず**、
pandas / requests / ddgs を使うツールが \`ModuleNotFoundError\` になります。
必要なライブラリを同梱した専用イメージを使うのはこのためです。

## 実行時の制限

サンドボックスは、ネットワークや書き込みを許可した場合でも次を必ず維持します。

- \`--read-only\`（ファイルシステムは読み取り専用。作業ディレクトリのみ例外）
- \`--rm\`（実行後にコンテナを破棄）
- メモリ・プロセス数・CPU の上限

追加でホストのファイルを見せたい場合は、**設定 › サンドボックス**でマウントを指定します。
`.trim(),
  },
  {
    slug: 'llm',
    title: 'LLM の接続',
    en: 'Models',
    summary: 'ローカルLLM（LM Studio）と API（Claude / OpenAI 互換）の登録方法。',
    body: `
## プロファイルという考え方

接続先は「プロファイル」として**何件でも保存**できます。1件が次を丸ごと持ちます。

- 接続先の種別（ローカル / API）
- エンドポイント（Base URL）
- APIキー
- モデル名
- 最大出力トークン数
- Thinking を無効化するかどうか

たとえば「Claude（API）」「ローカルA」「ローカルB（別ホスト）」を並べて保存し、
実行のたびに選べます。

## どれが使われるか

優先順位は次のとおりです。

1. 実行時に選んだもの（⌘K ランチャー）
2. タスクごとに保存した既定
3. 設定 › モデル で選んだ既定（★ の付いた行）

## ローカルLLM（LM Studio）

1. LM Studio でモデルを読み込み、\`Developer\` タブでサーバを起動する
2. **設定 › モデル › ＋ モデルを追加**
3. 接続先「ローカルLLM」、Base URL に \`http://localhost:1234/v1\`
   （VM から母艦につなぐ場合は \`http://10.0.2.2:1234/v1\`）
4. モデル名は LM Studio に表示されている識別子をそのまま
5. 「接続テスト」で疎通を確認

## API（Claude）

1. 接続先「API」→ プロバイダ種別「Anthropic」
2. モデル名（例: \`claude-sonnet-5\`）と APIキーを入力
3. Base URL は不要です

OpenAI 本家・Bedrock Gateway・LiteLLM などは「OpenAI互換」を選び、Base URL を入れます。

## 最大出力トークン数

**2500 未満にしないでください。** Thinking を持つモデル（Gemma 4 等）が
内部思考だけで上限を使い切り、**回答が空になる**ことが実際に起きています。
3000〜4000 を推奨します。

ただしサーバ側の Context Length を超えても意味がないので、
LM Studio の \`Load\` タブの設定も合わせて確認してください。

## APIキーの扱い

APIキーは \`agent_studio.db\` に**平文で保存**されます（\`.gitignore\` 済み）。

画面には**一度保存した鍵は表示されません**。「登録済み」とだけ出ます。
変更したいときだけ入力してください。空のまま保存しても鍵は消えません。
`.trim(),
  },
  {
    slug: 'strategy',
    title: '実行方式の選び方',
    en: 'ReAct / StateMachine',
    summary: '2つの動かし方の違いと、どちらを選ぶべきか。',
    body: `
## 2つの方式

### ReAct ループ（汎用）

毎ステップ、LLM が次の行動（ツール実行・コード生成・完了）を自分で決めます。

- 手順が事前に読めない作業向き
- コード生成・ファイル操作・保存済みツールの実行ができる
- ステップ数が読めず、コストが変動する
- 書式（\`THOUGHT:\` / \`ACTION:\`）を守れない小型モデルだと空回りする

### リサーチ（ステートマシン）

検索 → 本文取得 → 充足判定 → 執筆を**固定の工程**で回します。

- 調査・レポート作成向き
- 検索と本文取得はコード側が実行するので、書式崩れで空回りしない
- LLM に任せるのは調査計画・充足判定・執筆の3つだけ
- **小型のローカルモデルでも安定する**
- ツール実行やコード生成はできない

## 選び方

| やりたいこと | 方式 |
| --- | --- |
| 調べてレポートにまとめる | リサーチ |
| 保存済みツールを組み合わせる | ReAct |
| コードを書いて動かす | ReAct |
| 小型ローカルモデルで安定させたい | リサーチ |

## どこで指定するか

- **設定 › グラフ** … 全体の既定
- **タスクの編集画面** … そのタスクの既定
- **⌘K ランチャー** … 今回だけ（\`⌘E\` で切り替え）

## 実行後に何が見えるか

実行詳細（履歴から実行を開く）で3つのビューを切り替えられます。

- **タイムライン** … 思考・ツール実行・観測を1本の流れで
- **グラフ** … 通ったノードと差し戻しの図
- **生ログ** … どこで時間を使ったかの帯
`.trim(),
  },
  {
    slug: 'search',
    title: '検索プロバイダ',
    en: 'Search',
    summary: 'エージェントが Web 検索に使うサービスの選択。',
    body: `
## 選べるもの

| プロバイダ | APIキー | 備考 |
| --- | --- | --- |
| DuckDuckGo | 不要 | 既定。すぐ使えるが結果は不安定なことがある |
| Tavily | 必要 | 検索結果の要約付き。エージェント用途に向く |
| Google CSE | 必要 | **2026年時点で新規受付終了**。既存キーがある場合のみ |
| Brave | 必要 | |

**設定 › 検索**で切り替えます。選んだプロバイダのキー入力欄だけが出ます。

## キーの扱い

保存したキーは画面に表示されません（「登録済み」とだけ出ます）。
変更したいときだけ入力してください。

検索用のキーは、ネットワークを許可したツール実行時に**環境変数としてのみ**
サンドボックスへ渡されます。LLM のAPIキーは渡されません。

## 検索結果が悪いとき

- スニペットは200〜250文字で切られます。エージェントは上位ページの
  **本文まで自動で取りに行きます**が、取得に失敗するページもあります
- 実行詳細のタイムラインで、実際に何を検索して何が返ってきたかを確認できます
- 同じクエリを繰り返している場合は自動で抑止し、別の観点を促します
`.trim(),
  },
  {
    slug: 'secrets',
    title: 'APIキーの保管',
    en: 'Secrets',
    summary: 'どこに、どういう形で保存されるか。持ち出さないための決め事。',
    body: `
## どこにあるか

APIキーは設定DBに**平文で**保存されます。

\`\`\`
~/.local/share/agent-studio/agent_studio.db
\`\`\`

環境変数 \`AGENT_STUDIO_DB\` を設定すると、そちらが優先されます。

## なぜ平文なのか

暗号化しても、復号する鍵をどこかに平文で置くことになります。
毎回パスフレーズを入力する方式にすると、定期実行（スケジューラ）が
動かなくなります。**見かけ上の安全と引き換えに機能を失うだけ**なので、
平文のまま「置き場所と権限」で守る方針にしています。

これは特別な判断ではありません。AWS CLI（\`~/.aws/credentials\`）、
gh CLI、npm も同じで、いずれも平文＋パーミッション 0600 です。

## 守っていること

| 対策 | 内容 |
| --- | --- |
| パーミッション | \`0600\`（所有者だけが読める）。DBに触るたびに設定し直す |
| 置き場所 | リポジトリの外。ディレクトリごとのコピー・同期・マウントに巻き込まれない |
| \`.gitignore\` | \`**.db\` で除外済み。追跡もされていない |
| サンドボックス | **DBが見えるマウント指定は自動で却下**（親ディレクトリ指定も含む） |
| API | 保存した鍵は \`GET\` で返さない。「登録済み」とだけ表示する |
| 実行履歴 | 使ったモデル名は残すが、鍵は残さない |

## サンドボックスへのマウントを止めている理由

コンテナの中で動くのは **AI が生成したコード**です。DBが見える状態にすると
「生成コードが鍵を読んで外へ送る」経路が開きます。ネットワークを
許可した実行なら、そのまま持ち出せてしまいます。

そのため **設定 › サンドボックス** で DB（またはその親ディレクトリ）を
指定しても、マウントされません。画面に却下の理由が出ます。

## 自分で気をつけること

- **DBを他人に渡さない。** バックアップやスクリーン共有も同じです
- 鍵が漏れたと思ったら、**プロバイダ側で失効させる**のが先です。
  こちらの設定を消しても、既に漏れた鍵は無効になりません
- 複数人で使うマシンなら、そもそもこのツールを共有アカウントで
  動かさないでください。パーミッションは同一ユーザーからは守れません
`.trim(),
  },
  {
    slug: 'startup',
    title: '起動と構成',
    en: 'How it runs',
    summary: 'この画面がどう動いているか。起動コマンドとポート。',
    body: `
## 起動

\`\`\`bash
python -m api
\`\`\`

→ http://localhost:8000

1プロセスで、この画面と API の両方を配ります。
ポートを変えるなら \`PORT=9000 python -m api\`。

## 構成

- **この画面** … React（ブラウザの中で動く）
- **サーバ** … FastAPI（Python）。データの読み書きとエージェントの実行
- **保存先** … \`agent_studio.db\`（SQLite）

Python はエージェント本体を動かし、画面のファイルを配り、API を提供します。
画面の描画はブラウザ側が行います。

## UIのコードを直す場合

ターミナルを2つ使います。

\`\`\`bash
uvicorn api.main:app --reload --port 8000   # ①API
cd web && npm run dev                        # ②画面（http://localhost:5173）
\`\`\`

保存するたびに画面へ反映されます。この場合 \`npm run build\` は不要です。

通常起動に戻すときは \`cd web && npm run build\` を1回実行してください。
やり忘れると古い画面のままになります。

## 旧 UI（Streamlit）

\`\`\`bash
streamlit run app_streamlit.py    # http://localhost:8501
\`\`\`

**同じデータベースを見ます**ので、同時に立ち上げて見比べられます。

## ブラウザを閉じても実行は続く

エージェントの実行はサーバ側のバックグラウンドで走ります。
画面を閉じても止まりません。戻ってきたら実行履歴から続きを確認できます。
`.trim(),
  },
]

export function findTopic(slug: string | undefined): HelpTopic | undefined {
  return HELP_TOPICS.find((t) => t.slug === slug)
}
