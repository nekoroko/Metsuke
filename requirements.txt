# アプリ本体の依存
langchain
langchain-openai
langchain-anthropic
langgraph
streamlit>=1.29        # st.container(border=True) を使用
apscheduler

# エージェントの web_search ツール（tools.py）がホスト上で直接使う。
# AI生成ツールが使うライブラリは requirements-tools.txt 側に定義されており、
# そちらはサンドボックスイメージにのみインストールされる
# （生成コードはすべてコンテナ内で実行されるため、ホストには不要）。
ddgs
