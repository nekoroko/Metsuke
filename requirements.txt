# アプリ本体の依存
langchain
langchain-openai
langchain-anthropic
langgraph
streamlit>=1.36        # st.navigation / st.Page を使用（app_streamlit.py。移行期の比較用に残す）
apscheduler

# Web UI（React SPA）を配る API サーバ。api/ 以下で既存モジュールを薄く包む。
fastapi
uvicorn[standard]
sse-starlette

# エージェントの web_search ツール（tools.py）がホスト上で直接使う。
# AI生成ツールが使うライブラリは requirements-tools.txt 側に定義されており、
# そちらはサンドボックスイメージにのみインストールされる
# （生成コードはすべてコンテナ内で実行されるため、ホストには不要）。
ddgs
